//Targeting the DOM

const theExistingMainElement = document.querySelector("#container")

//The #container is your way of specifying that you want an HTML element whose id attribute equals "container". That just so happens to be this element.
